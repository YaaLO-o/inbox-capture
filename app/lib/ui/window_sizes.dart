/// 悬浮窗口的两种尺寸。
class WindowSizes {
  /// 已配置 Vault：圆形采集入口、拖动把手与状态区。
  static const pillWidth = 132.0;
  static const pillHeight = 132.0;

  /// 未配置 Vault：放大显示选择引导。
  static const onboardingWidth = 420.0;
  static const onboardingHeight = 300.0;

  /// 菜单触发更新：显示检查、下载与安装进度。
  static const updateWidth = 420.0;
  static const updateHeight = 300.0;
}
